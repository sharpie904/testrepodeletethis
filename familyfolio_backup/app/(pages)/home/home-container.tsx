"use client"

import {useRoles} from "@/app/state/useRoles";

const HomeContainer = ({familyID}: { familyID: string }) => {
    const {setUserRole} = useRoles();
    return <></>;
}

export default HomeContainer;